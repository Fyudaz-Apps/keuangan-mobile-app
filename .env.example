# Application Settings
APP_NAME=keuangan-mobile-app
APP_VERSION=1.0.0
ENVIRONMENT=development

# User Preferences
DEFAULT_CURRENCY=IDR
DEFAULT_LANGUAGE=id
DEFAULT_THEME=light

# Gemini AI Configuration (fallback key; the app Settings screen can override per device)
EXPO_PUBLIC_GEMINI_API_KEY=your_gemini_api_key_here
